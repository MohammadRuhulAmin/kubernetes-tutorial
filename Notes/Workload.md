Workload ? 

A `workload is an application running inside kubernetes` where we run it inside a set of pods. 

Workload Resources: Kubernetes uses the resources (Deployment, ReplicaSet, StatefulSet, DaemonSet) to manage pod for
running application or service. 

<table border=1px>
    <tr><td>Workload Resource</td> <td>Description </td></tr>
    <tr><td>Deployment</td> <td> For managing stateless application </td></tr>
    <tr><td>Deployment</td> <td> For managing stateless application </td></tr>
</table>