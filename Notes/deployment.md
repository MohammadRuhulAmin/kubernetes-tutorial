Deployment:
Deploy in production environment, we need Deployment. We want rolling updates. 


Deployment --> Replicaset --> Pod

Inside a Deployment there is a Replicaset, and inside a Replicaset there are multiple pods.

Definition of Deployment:

Selector and template metadata name has tobe same. 

```sh


```